load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
    name = "doctest",
    textual_hdrs = glob(
        ["doctest/**/*.h"],
        exclude = ["doctest/parts/**/*"],
    ),
    visibility = ["//visibility:public"],
)
