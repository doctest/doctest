language: cpp
notifications:
  email:
    on_success: change
    on_failure: change
compiler:
  - gcc
  - clang
before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -y -qq lcov curl valgrind
script:
  - cmake examples/dev_testing
  - make
  - ./exe
  - valgrind --leak-check=full --track-origins=yes --error-exitcode=1 ./exe
