module(name = "doctest_examples")

bazel_dep(name = "rules_cc", version = "0.0.8")
bazel_dep(name = "bazel_skylib", version = "1.4.2")
bazel_dep(name = "doctest")

local_path_override(
    module_name = "doctest",
    path = "..",
)
